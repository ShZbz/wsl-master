# CHANGELOG

## v0.1.4 (2026-09-11)

清理判定与执行重写轮（对应内部 v3.2.5）：删除前统一安全闸门 + 规则派生快速扫描 + 清理看板，并修复树图嵌套模式的黑块与扁平模式单色问题。

### 修复 · 清理（漏删）
- **预览截断即删除范围**：预览 `ORDER BY size DESC LIMIT 500`，10 万级垃圾文件界面只能看见/删掉前 500 个 → 预览与执行同源，统计全量、仅列表截断展示
- **分类写死**：清理页只认 4 个固定分类，其它分类既不显示也不可删 → 分类按钮由服务端返回动态生成
- **快速扫描范围漂移**：扫描根目录在 Python 与 Rust 两侧各写死一份，缺 `~/.npm/_cacache`、`~/.cargo/registry/cache`、`~/.cache/uv` 等 → 改为由规则文件派生（`rules/engine.py::quick_scan_roots()`），规则与扫描范围单一真值来源
- **CLI 只删前 100 条**：`clean` 命令直接取 DB 里 category 非空的前 100 条删除，第 101 名之后永远删不到 → 改为走同一套安全闸门 + 全量候选集
- **`whitelist` 形同虚设**：YAML 中声明的白名单（`wtmp/btmp`、`/etc`、`~/.ssh` 等）没有任何代码执行，名义受保护实际无人拦 → Rust/Python 两侧真正生效

### 修复 · 清理（错删）
- **新增 `clean/policy.py`：删除前的唯一权威闸门 `CleanPolicy`**。依次判定：白名单 → wsl-master 自身数据 → 按当前规则重算分类 → Caution 需显式确认 → 文件已消失或已变成目录（绝不再 `rmtree` 连子树删掉）→ 被进程打开或 mmap → 位于虚拟环境 / Git 仓库内 → 无写权限 → `/tmp` 中 10 分钟内仍在写入。不通过即拦截，并给出人话原因
- **隔离区扁平存放同名覆盖** → 改为 `quarantine/<run_id>/<原始绝对路径>` 镜像 + 实时 `manifest.jsonl`；同名多份还原时拒绝猜测
- **删除范围受列表长度影响** → 执行按「分类范围 + 单独勾选/取消」在**全量候选集**上重算（新增 `clean/planner.py`）

### 修复 · 树图
- **嵌套模式实心黑块**：`max_abs_depth = depth + 2` 与深层收集需求不匹配 → 深层目录拿不到子节点 → JSON 无 `children` → 前端画空底色（连通域分析确认 19 块实心矩形颜色恰为 `#1e3050` 叠涂三层）。改为对空目录做定向索引范围回填：`get_tree` 0.004s（放宽深度窗口的朴素解为 0.081s→1.24s，未采用）
- **SQLite 索引失效**：`parent_path = ? OR parent_path LIKE ? ESCAPE '\'` 因 OR + ESCAPE 放弃索引走全表扫（23 次查询 5.7s）→ 改纯范围谓词 `[path+'/', path+'0')`，毫秒级
- **扁平模式太黑太单调**：`drawTreemap` 对文件夹写死 `#1e3050`、完全不用后端算好的分类色，而扁平模式返回的全是文件夹 → 94.85% 面积为同一颜色。改为「没有内容可展开的文件夹」按分类色画实心块，嵌套模式的残留空目录一并解决
- **零字节节点 NaN**：会让 squarify 算出 `0/0` 静默污染整行布局；同时放宽 `_maxRects` 预算，防回填后节点变多导致超预算目录退回实心块

### 功能
- 清理页看板四数字：**选中垃圾总大小** / **可删除文件总大小** / **扫描发现垃圾总量** / **⚠️需确认·🔒已拦截**（取消勾选即时联动）
- 扫描结束后自动加载并全选「删除后无影响」的 Safe 文件；每个分类 ☑仅Safe → ☑全选 → ☐未选 三态循环，单个文件可单独勾选
- 被闸门拦下的文件仍留在列表中（🔒 + 原因），不静默消失；选中含 ⚠️ 时弹明细确认 + 总数确认双重确认
- CLI `clean` 新增 `--caution`（显式确认后一并清理 Caution 文件），输出闸门放行/拦截统计与原因分类

### 性能
- 树图最密视图绘制 22.5ms → 11.1ms；嵌套实心黑块 19 → 0；扁平模式平均亮度 27.3 → 76.7、暗像素占比 99.3% → 0.2%

### 测试
- 108 → 151 个 Python 测试（新增清理闸门/计划器与树图回归）；Rust 13 → 15 个；`cargo clippy -D warnings` 干净

## v0.1.3 (2026-09-01)

CI 修复轮（对应内部 v3.2.4）。

### 修复
- **CI 红灯**：`test_fallback_skips_excluded_roots` 在 GitHub Actions 上失败（本地全绿）——该测试把 `os.path.exists` 全局替换为恒真，连带骗过 `RulesEngine.from_default()` 的规则文件守卫，fallback 线程内 `FileNotFoundError`，done 事件永不触发，30s 超时。测试改为只对 `/mnt/x` 说谎，其余委托真实实现
- **规则文件路径硬编码（生产隐患）**：`from_default()` 写死开发机路径 `/opt/wsl-master/...`，且包内候选指向不存在的 `wsl_master/config/`——除开发机外任何环境 fallback 均静默加载空清理规则。改为源码仓根相对 + 工作目录相对双候选

## v0.1.2 (2026-09-01)

对应内部版本 v3.2.3：全模块复查修复轮 + 扫描性能优化。

### 修复
- **备用扫描器数据丢失**：fallback 流程全程无 `commit()`，扫描结果留在未提交事务里——其它连接永远看不到，进程退出整体回滚，扫描"成功完成"但 Web/CLI 查不到任何结果；同秒重扫触发 UNIQUE IntegrityError
- **非 root 启动崩溃**：`/var/log` 属 root 时 import 期即抛 PermissionError；日志/隔离目录按 `/var/log/wsl-master → ~/.local/state/wsl-master → /tmp` 写探针回退，支持 `WSL_MASTER_LOG_DIR`/`WSL_MASTER_QUARANTINE` 环境变量
- **排除模式静默失效**：Rust `glob_to_regex` 把 `**` 编译成仍跨不过 `/` 的模式，`**/selfcheck/**` 类排除规则从未匹配过任何路径；Python `fnmatch` 的 `*` 又会跨越 `/`——双端统一为组件级 glob，两端单测锁定语义
- **重叠扫描根双计数**：同一路径传两次所有文件记双份；两个扫描器均加排序去重 + 嵌套根剔除
- **显式扫描 `/mnt/...` 行为分裂**：Rust 静默跳过、Python 备用扫描器却去走 9p 近乎挂死；现在双端一致跳过且 Rust 明确打印跳过原因
- **WebUI 假超时永久卡死**：目录密集段/写库段被误报"扫描无响应"后永久停止轮询；活动签名改为 文件数|大小|当前路径|进度，写库阶段显示"正在写入数据库..."，超时只提示不停轮询
- **停止扫描后误显示"扫描完成"**：idle 态如实显示"已停止"
- **`--paths` 逗号拼接**损坏含逗号路径：改为重复标志传参
- **回调异常击穿扫描状态机**：一个签名不符的回调把正常扫描打成 ERROR；新增回调守卫
- 杂项：`/api/tree?top_n=abc` 等坏参数 500 → 容错回退；浏览器断连 BrokenPipe 刷屏 → 静默；跨设备隔离目录 `copy2` 崩溃 → `shutil.move`

### 性能
- Rust walker 热路径：每条目省一次 path 拷贝 + 一次相对路径 String 分配（30 万文件约省 60 万次堆分配）
- 权限错误进度 500ms 限速（原本每错误穿透三层通道各推一条 JSON）
- 聚合器去掉全量节点深拷贝：自底向上大小传播改 8 字节索引排序
- 备用扫描器单次 stat 判定（删掉多余的 `isfile()` 二次 stat），符号链接语义与 Rust 对齐

### 测试
- 92 → 108 个测试（新增 16 个 v3.2.3 回归测试）；Rust 13 个测试；`cargo clippy -D warnings` 干净

## v0.1.1 (2026-08-31)

### 工程
- 修复 CI:scanner 通过 `cargo clippy -- -D warnings`(清理 2 个死字段、复杂类型改 type alias、文档注释缩进、循环迭代写法)——首次发布后 CI rust 作业变绿

## v0.1.0 (2026-08-31)

首次公开发布（对应主仓 v3.2.2 能力基线）。

### 功能
- 交互式树图磁盘分析：嵌套/扁平双模式、缩放导航、面包屑、`Other` 小文件聚合
- 四类垃圾清理：包管理器缓存/系统日志/临时文件/应用缓存，YAML 规则驱动，dry-run 预览 + 回收区删除
- VHDX 收缩助手：Windows Docker Desktop 场景自动探测 + PowerShell 脚本生成
- Web UI：单文件前端、keep-alive HTTP 层、独立 Worker 渲染
- 可选 Rust 扫描引擎（`scanner/`），大目录扫描显著提速

### 性能
- 全盘扫描去掉无谓排序，冷/热缓存分别提速 ~39% / ~20%（29.9 万文件实测 55.9s → 34.3s 冷、40.6s → 32.5s 热）
- 树图接口巨型 IN 列表改临时表 + top-N 子查询（984ms → 350ms）
- 清理预览类别预计算 + 缓存（1085ms → 首次 136ms / 后续 9ms）

### 修复
- 树图小黑块：经典短边排布 + 布局前聚合 + 小矩形去描边（近黑块 37% → 0）
- API 死循环：路径回溯在根目录 `dirname('/')=='/'` 时无限循环（挂死请求）
- 叶子目录多文件只返回首文件、重启后 Web UI 全 500（DB 目录/schema 缺失）
- CLI 不传 `--db` 崩溃、成功扫描误标 ERROR（wal_checkpoint 超时）
- 中止扫描残留全零占位行、Python 兜底扫描器 `size_self` 恒 0 等三处
- 前端 XSS/HTML 注入、键盘劫持、非法 JSON 未捕获